settings:
  legacy:
    force: false
    interval: 0s
schema:
- name: API
  path: .
  commands: 
    run:
      method: "go run main.go"
      status: true
  watcher:
    extensions:
    - go
    paths:
    - /
    ignored_paths:
    - .git
    - .realize
    - vendor
